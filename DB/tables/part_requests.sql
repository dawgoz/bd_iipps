CREATE TABLE "PART_REQUESTS"
(
    "ID"                 NUMBER(8, 0)
                            GENERATED BY DEFAULT ON NULL AS IDENTITY
                                MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH          1 CACHE 20 NOORDER NOCYCLE NOKEEP NOSCALE
                            NOT NULL ENABLE
   ,"ORDER_ID"           NUMBER(8, 0) NOT NULL ENABLE
   ,"PART_NAME"          VARCHAR2(20) NOT NULL ENABLE
   ,"DESCRIPTION"        VARCHAR2(254)
   ,"SUBMISSION_DATE"    DATE
   ,"SID"                VARCHAR2(20)
   ,CONSTRAINT "PART_REQUESTS_PK" PRIMARY KEY("ID") USING INDEX ENABLE
);

CREATE UNIQUE INDEX "PART_REQUESTS_PK"
    ON "PART_REQUESTS"("ID");

ALTER TABLE "PART_REQUESTS"
    ADD CONSTRAINT "PART_REQUESTS_ORDERS_FK" FOREIGN KEY("ORDER_ID") REFERENCES "ORDERS" ("ID") ON DELETE SET NULL ENABLE;